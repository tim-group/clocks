pluginManagement {
    repositories {
        maven {
            name "nexus"
            url "${repoUrl}/groups/public"
        }
    }
}

rootProject.name = "clocks"

include 'joda'
include 'testing'
include 'joda:testing'
